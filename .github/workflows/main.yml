name: quizlet_push
on:
  workflow_dispatch:
    branches:
      - master
  push:
    paths:
      - 'formatted/*'
      - 'backup/*'

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/actions/containerized
